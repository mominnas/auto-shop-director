<Page
    x:Class="MMN.App.Views.VehicleDetailPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="using:MMN.App.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <StackPanel Margin="32">
            <TextBlock Text="Add/Edit Vehicle" FontSize="24" Margin="0,0,0,16"/>
            <TextBox Header="Make" Text="{x:Bind ViewModel.Make, Mode=TwoWay}" Margin="0,8"/>
            <TextBox Header="Model" Text="{x:Bind ViewModel.ModelName, Mode=TwoWay}" Margin="0,8"/>
            <TextBox Header="Year" Text="{x:Bind ViewModel.Year, Mode=TwoWay}" Margin="0,8"/>
            <TextBox Header="VIN" Text="{x:Bind ViewModel.VIN, Mode=TwoWay}" Margin="0,8"/>
            <StackPanel Orientation="Horizontal" Margin="0,16,0,0" Spacing="8">
                <Button Content="Save" Click="SaveButton_Click"/>
                <Button Content="Cancel" Click="CancelButton_Click"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>